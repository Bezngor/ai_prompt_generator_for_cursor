# Telegram-бот для генерации промптов AI

Telegram-бот на Python (aiogram 3.x) для создания качественных промптов для AI-моделей (Cursor, ChatGPT, Claude и др.) с использованием итеративного интерактивного воркфлоу.

## 🎯 Возможности

- **5-фазный воркфлоу** создания промптов:
  1. Сбор информации о задаче
  2. Уточняющие вопросы и анализ ответов
  3. Генерация рекомендаций
  4. Создание финального промпта
  5. Итеративное улучшение и экспорт

- **Интерактивный диалог** с использованием FSM (Finite State Machine)
- **Интеграция с ProxyAPI** для работы с OpenAI-совместимыми моделями
- **Экспорт промптов** в текстовые файлы
- **Редактирование секций** промпта
- **Inline-кнопки** для удобной навигации

## 🏗️ Архитектура

```
tg_bot_prompt_generator/
├── main.py                  # точка входа, handlers
├── config.py                # конфигурация, переменные окружения
├── states.py                # FSM состояния (StatesGroup)
├── llm_client.py            # класс для взаимодействия с LLM
├── session_manager.py       # хранение сессий и истории
├── utils.py                 # утилиты (форматирование, парсинг)
├── templates.py             # шаблоны вопросов, рекомендаций, промптов
├── requirements.txt         # зависимости
└── README.md                # инструкции по запуску
```

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repo_url>
cd tg_bot_prompt_generator
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Token
BOT_TOKEN=your_telegram_bot_token

# OpenAI/ProxyAPI
OPENAI_API_KEY=your_proxyapi_key
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
LLM_MODEL=gpt-4o-mini

# Логирование
LOG_LEVEL=INFO
```

**Как получить токены:**
- **BOT_TOKEN**: Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
- **OPENAI_API_KEY**: Получите ключ на [ProxyAPI](https://proxyapi.ru/)

### 5. Запуск бота

```bash
python main.py
```

### 6. Использование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Следуйте инструкциям бота

## 📋 Команды бота

- `/start` — начать создание промпта
- `/help` — справка по командам
- `/restart` — начать заново
- `/accept` — принять рекомендации
- `/rethink` — пересчитать рекомендации
- `/edit` — редактировать описание задачи
- `/edit_section` — отредактировать секцию промпта
- `/add_requirement` — добавить требование
- `/remove_requirement` — удалить требование
- `/save` — сохранить промпт
- `/export` — скачать промпт как .txt файл

## 🔄 Процесс работы

1. **Описание задачи**: Пользователь описывает задачу, которую хочет решить
2. **Уточняющие вопросы**: Бот генерирует 3-4 вопроса для уточнения деталей
3. **Ответы**: Пользователь отвечает на вопросы по одному
4. **Рекомендации**: Бот анализирует ответы и генерирует рекомендации по:
   - Технологическому стеку
   - Архитектуре
   - Ключевым особенностям
   - Масштабируемости
   - Compliance и безопасности
   - Рискам
5. **Генерация промпта**: После принятия рекомендаций бот создает финальный промпт
6. **Редактирование и экспорт**: Пользователь может редактировать промпт и экспортировать его

## 📁 Структура промпта

Сгенерированный промпт содержит следующие секции:

- `# ЗАДАЧА / # GOAL` — четкое описание цели
- `# ТРЕБОВАНИЯ / # REQUIREMENTS` — детальные требования
- `# TECH STACK` — технологический стек с обоснованием
- `# АРХИТЕКТУРА` — структура проекта, модули, паттерны
- `# ДОПОЛНИТЕЛЬНЫЕ ТРЕБОВАНИЯ` — специфические требования
- `# OUTPUT FORMAT` — как должны быть структурированы результаты

## 🛠️ Технологии

- **Python 3.10+**
- **aiogram 3.x** — фреймворк для Telegram
- **OpenAI** (через ProxyAPI) — генерация текста и анализ
- **python-dotenv** — управление переменными окружения
- **pydantic** — валидация данных

## 📝 Логирование

Все события логируются в файл `bot.log` и выводятся в консоль. Уровень логирования настраивается через переменную `LOG_LEVEL` в `.env`.

## ⚠️ Требования

- Python 3.10 или выше
- Активный интернет-соединение
- Токен Telegram бота
- API ключ ProxyAPI

## 🔒 Безопасность

- **НЕ коммитьте** файл `.env` в репозиторий
- Храните токены в безопасном месте
- Используйте переменные окружения для конфиденциальных данных

## 📄 Лицензия

Этот проект создан для образовательных целей.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в файле `bot.log`
2. Убедитесь, что все переменные окружения настроены правильно
3. Проверьте подключение к интернету и доступность ProxyAPI

## 📝 Примечания

- Экспортированные промпты сохраняются в папке `exports/`
- Сессии хранятся в памяти и очищаются при перезапуске бота
- Для постоянного хранения можно добавить интеграцию с БД (SQLite/PostgreSQL)

